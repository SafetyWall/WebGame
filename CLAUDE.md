# 게임 기획서 — 성장형 파티빌더 RPG

> 이 문서는 **제로에서 구현을 시작하기 위한 기획서**다. 구현 코드/진척 상태는 담지 않는다.
> 수치는 모두 **시작 제안값**이며, 구현 후 시뮬레이터로 측정해 확정·조정한다.
> CLI(Claude Code 등)에서 CLAUDE.md 또는 design doc으로 두고 갱신하며 진행할 것.

---

## 1. 비전 / 한 줄 요약

랜덤한 적에 맞춰 **재화로 파티를 성장시키며(전직·강화·영입·슬롯확장)** 스테이지를 돌파하는,
**ATB(틱 기반 액티브 타임 배틀) 자동 전투**의 싱글 플레이 로그라이크 RPG. **텍스트 기반**으로 충분.

깊이의 출처는 *전투의 운*이 아니라 **(1) 성장 선택의 다축 트레이드오프**와 **(2) 직업 간 카운터 관계망**이다.

---

## 2. 디자인 기둥 (핵심 원칙 — 모든 결정의 기준)

### 2.1 장르 정체성 (흔들리지 말 것)

- **성장형 파티빌더 RPG.** 내가 키운 파티에 애착이 생기는 육성 게임.
- **싱글 플레이, 런(run) 단위 로그라이크.** 지면 런이 리셋되고 처음부터.
- **성장이 1차, 출전 선발은 부차.** "키운 애들 중 누구를 데려갈까"(선발)도 재미지만, 핵심은 *재화로 키우고 늘려가는 성장*이다. "유닛 풀 >> 슬롯 → 무엇을 버릴까"의 강제 선별을 중심으로 두지 않는다.
- **TFT/오토체스가 아니다.** 선별형 게임이 아니다.
- **전장 드래프트가 아니다.** "고정 인원 반 가르기 수싸움"이 아니다.

### 2.2 "정답 맞추기"를 깨는 것이 핵심 과제

이 게임의 거의 모든 메커니즘은 "정답 맞추기"(룩업)를 피하기 위해 존재한다.

> **정답 맞추기의 정의:** 입력(몹) 고정 + 선택지 고정 + 결과 결정론적 → 반드시 최적해가 하나로 존재하고, 한 번 찾으면 끝.

"캐릭터 성장 없음", "고정 시너지", "매 턴 같은 행동 반복"은 전부 이 공식의 *부분 증상*일 뿐. 부분을 고쳐선 안 되고 **공식 자체를 깬다.** 깨는 길은 셋:

- **A. 운(결과 비결정론).** → 단, "빌드 맞았는데 운으로 짐"의 *나쁜 랜덤*은 금지. 경직성 푸는 윤활유로만(2.3).
- **B. 자원 경제(제약 아래 선택).** 최적을 알아도 골드 부족·비가역·기회비용 때문에 *못 씀* → "가진 걸로 차선에 어떻게 근접하나"가 매번 다른 문제. ← **깊이의 1차 출처(4장).**
- **C. 동적 입력(상태 변하는 적·전투).** 단, 완전 자동 전투에선 "더 큰 정답표"가 될 위험 → **ATB + 마나/쿨의 "예측 어려운 결정론적 변동성"**이 C의 안전한 형태(5장).

> 주의: "스킬을 복잡하게"는 깊이의 출처가 아니다. 자동 전투에선 행동이 복잡해도, 시작 누른 뒤 결과가 정해져 있으면 "더 큰 자판기"일 뿐. 깊이는 행동 복잡도가 아니라 **내 결정이 결과에 계속 영향을 주는가**에서 온다(체스: 말은 단순해도 매 수가 새 결정이라 깊다).

**결론: 깊이의 1차 출처 = B(성장 경제). 전투 변수(ATB·스킬·관계망)는 보조.** 전투가 단순 자동이어도 성장 경제가 다축이면 게임은 깊다.

### 2.3 운(랜덤)에 대한 입장

- **나쁜 랜덤(결과 흔들기) = 금지.** "빌드 맞았는데 운으로 짐"은 전략감을 죽인다.
- **좋은 랜덤(경직성 푸는 윤활유) = 환영.** 완전 결정론이면 "이 로스터론 이 적 절대 못 깸"이 *확정*돼 도전 여지가 사라진다. 약간의 변동성은 불리한 매치업을 "한 줌의 승률"로 만들어 도전 가능하게 한다.
- 우리 변동성은 순수 RNG가 아니라 **ATB 타이밍 + 마나/쿨 게이팅**에서 나온다(결정론적이지만 복잡). 결과적으로 매치업이 "0% or 100%"가 아니라 "23%, 67%" 같은 **승률 분포**가 된다.

### 2.4 기각한 방향 (재론 방지용 기록)

- **드래프트(매판 무작위 제시 영입):** 깊이는 늘지만 무게중심이 *성장*에서 *매판 조합 수싸움*으로 이동 → "더 깊은 다른 게임"이 됨. 궁금하면 별도 프로젝트로.
- **고정 시너지(일방향 "A가 B를 강화"):** 그 자체론 "더 복잡한 정답표"일 뿐. → 카운터의 카운터 관계망(6장)으로 간다.
- **스킬 가중치 랜덤 발동:** "회복 급한데 평타 쳐서 짐"의 나쁜 랜덤. → 우선순위 + 마나/쿨 게이팅(5.5)으로 변동성을 만든다.
- **출전 슬롯 하드 상한:** 인위적 벽 대신 **체증 비용**으로 기회비용이 브레이크를 걸게 한다.

---

## 3. 핵심 게임 루프

1. **랜덤 몹 등장** — 스탯 + 특수 속성(가위바위보 카운터)을 보여준다.
2. **준비 단계** — 골드로 구매: 영입(로스터+) / 강화(레벨+) / 전직(직업, **비가역**) / 슬롯 확장(출전+1, **체증**).
3. **출전** — 로스터에서 현재 슬롯 수만큼 선발. 이번 적에 맞춰 적응.
4. **자동 전투** — ATB로 진행. 플레이어 인게임 개입 없음. 결과는 라운드 스냅샷으로 표시.
5. **보상** — 승리 시 골드(향후 유물). 패배 시 **런 리셋**.
6. 반복. 클리어 목표 스테이지 수는 시작값으로 5, 조정 가능.

---

## 4. 성장 경제 (깊이의 1차 출처)

### 4.1 골드 사용처 4종

| 항목 | 효과 | 비용 (시작 제안값) | 트레이드오프 축 |
|---|---|---|---|
| 영입 | 노비스를 로스터에 추가 | 4 (고정) | 넓히기 |
| 강화 | 캐릭터 레벨 +1 (스탯↑) | 4 | 깊이기 |
| 전직 | 직업 변경(**비가역**) | 5 | 정체성 |
| 슬롯 확장 | 출전 가능 인원 +1 | **체증** `5 + 4*(현재슬롯-3)` (4번째=5, 5번째=9, 6번째=13…) | 넓히기(무대) |

- 시작값: 노비스 2명, 골드 5, 슬롯 3. 스테이지 클리어 보상 = `4 + 스테이지`.
- 슬롯 **하드 상한 없음**(체증이 자율 균형). 로스터는 UI 보호용 소프트 상한만(예: 10).

### 4.2 다축 트레이드오프 (정답 맞추기를 깨는 엔진)

- **넓히기(영입/슬롯) vs 깊이기(강화) vs 정체성(전직)** — 같은 골드를 어디에.
- **지금 적 vs 다음 적** — 이번 적엔 A가 정답인데, 다음을 보면 B 투자가 나음.
- **비가역** — 전직/2차전직은 못 무름. 초반 물리 몰빵 → 후반 물리면역에 곤란.
- 골드가 빠듯해 다 못 함(런 전체 액션 소수) → **무엇을 포기하느냐가 곧 빌드.**
- 슬롯 체증: "슬롯 하나 더가 이 값을 하나?"를 매번 저울질 → 기회비용이 멈춤 지점을 만든다.
- **다양성(슬롯/영입) ↔ 카운터 대응:** 넓고 다양한 파티는 속성 카운터(물리면역 등)에 강하고, 좁은 특화는 효율적이지만 엉뚱한 적에 막힘. 둘 다 정답이 아니라 *상황 따라 다름*.

### 4.3 승리 보상 누적 (향후 — 유물)

- 기본 누적은 "로스터 성장" 자체. 약하면 **유물** 도입 고려:
  - 단순 스탯이 아니라 *조건부 상호작용* 유물 ("탱 살아있으면 딜러 +20%", "물리면역 적에 마법딜 2배").
  - 유물 = 관계망과 같은 재료(조건부 규칙). 쌓이면 런만의 시너지 엔진.
  - **전투 밖 유물**(골드 보너스, 전직 할인)부터 도입하면 밸런스 위험이 낮다. 전투 개입 유물은 그다음.
  - 주의: 너무 많으면 "숫자 불리기 = 정답 맞추기 회귀". 양념으로.

---

## 5. 전투 시스템 — ATB (틱 기반)

### 5.1 기본 모델

- **완전 자동 전투.** 플레이어는 전투 전 빌드만 정하고, 전투는 스냅샷으로 본다.
- **틱(tick)이 유일한 시계.** 게이지·버프 만료·쿨다운·마나회복 전부 틱 기준.
- **행동력 임계치 = 1000.** 게이지가 1000 차면 1회 행동 후 리셋.
- **기준 속도 ≈ 100** → 1000 채우는 데 약 10틱. 빠른 유닛(150)은 ~7틱, 느린 탱(60)은 ~17틱마다 행동.
- **속도 = 틱당 게이지 charge량.** 빠른 유닛이 더 *자주* 행동(빈도 차이). 단순 순서 차이가 아니다.
- **1 라운드 = 25~30틱**(시작값). **순수 표시(스냅샷) 단위.** 전투 계산·밸런스에 영향 0. 라운드마다 "지금 전황" 로그를 끊어 보여줄 뿐. 길이는 로그 밀도 취향으로 자유 조정. 기준: 한 라운드에 평균 캐릭이 1~3회 행동하는 밀도.

> 틱 절대값은 무의미, 비율만 의미. 1000을 택한 이유: **속도 버프/디버프가 핵심 메커니즘**이라 세밀한 변조(예: +7%)를 표현할 해상도가 필요.

### 5.2 시뮬레이션 방식 — "매 틱 계산" 채택

- 매 틱: 각 유닛 `gauge += effSpeed`, 1000 넘은 유닛 행동 처리.
- 틱당 작업이 덧셈 수준이라 극도로 쌈(수천 회 시뮬도 1초 이내). 충분히 빠르다.
- **"다음 행동자 수식 점프(min-heap)" 방식은 채택 안 함.** 빈 틱을 건너뛰어 이론상 빠르지만, **속도 버프가 잦으면** "버프 발생/만료마다 큐 재계산"이 빈번해 오히려 복잡·비효율. 우리 게임은 속도 변조가 핵심이라 이득이 상쇄됨.
- **측정 전 최적화 금지.** 매 틱으로 만들고, 측정해 정말 느릴 때만 최적화.

### 5.3 속도 버프 처리 규칙 (시뮬 안정성 핵심)

1. **이미 찬 게이지를 소급 수정하지 말 것.** 속도 버프는 *앞으로 차는 속도*만 바꾼다. 매 틱 `gauge += base.spd * (1 + Σ활성속도버프)`로 계산하면 자동으로 맞음.
2. **매 틱 현재 속도로 재계산** → 버프가 도중에 걸리거나 풀려도 그 틱부터 자동 반영.
3. **틱 내 처리 순서 고정:** ① 버프 만료 검사 → ② 게이지 증가 → ③ 1000 넘은 유닛 행동. 순서 일관성이 결정론·재현성을 보장.

### 5.4 버프/디버프 지속시간

- **시간형:** 틱 기준. `{ type, value, expireTick: nowTick + 지속틱 }`. 매 틱 `nowTick > expireTick`이면 제거.
- **행동형:** 카운터 기준. `{ type, value, charges: N }`. 해당 유닛 행동 시 -1, 0이면 제거.
- 섞어 써도 각자 자기 기준으로 관리하면 안 엉킴.
- **라운드 기준으로 세지 말 것** — 라운드는 표시용 가짜 단위라 캐릭마다 체감 지속이 달라짐.

### 5.5 스킬 시스템

- **직업당 여러 스킬.** (역할 다양성의 진짜 출처. 속도만으론 "행동 빈도"만 다양해지지 "행동 종류"는 안 갈림.)
- **발동: 플레이어 지정 우선순위 + 결정론.**
  - 각 유닛이 스킬을 **우선순위 순서**로 가짐. 행동 차례마다 위에서부터 훑어 "지금 쓸 수 있는(마나 충분 + 조건 충족) 첫 스킬" 발동, 없으면 평타.
  - **우선순위를 플레이어가 세팅 → 그게 곧 빌드.** ("광역 우선" vs "단일 폭딜 우선")
- **마나 + 쿨타임이 게이팅 = 변동성의 진짜 엔진.**
  - 우선순위 1번이 *항상* 나가지 않음 — 마나 안 찼거나 쿨이면 2번, 3번… 평타로 흐름.
  - 마나가 *언제* 차느냐는 ATB(행동 빈도·피격)에 따라 매 전투 미세하게 달라짐.
  - **발동 규칙은 100% 결정론(추론 가능)인데 자원 상태가 복잡해 실질 예측이 어려움.** 순수 랜덤 없이 변동성 획득. 같은 직업/스킬로도 우선순위·레벨로 다른 빌드.
- **스킬 레벨업** = 성장 축(위력/마나효율/쿨 강화). 직업당 스킬 슬롯은 고정으로 주되 *운용(우선순위) + 투자(레벨)*로 정체성이 갈림.
- 쿨타임도 틱 기준(`다음 사용 가능 틱`)으로 관리 → 모든 게 한 시계로.
- (구현하며 정할 디테일: 마나 회복 방식 — 틱당? 행동당? 피격 시? / 쿨 단위 — 틱? 행동? / 우선순위 발동 시 게이지 일부 보존 여부.)

### 5.6 타게팅 규칙

- **딜러:** 적(단일 몹)을 공격.
- **사제(힐러):** 현재 HP 최저 아군을 회복.
- **몹 단일 공격:** 탱(가디언)이 살아있으면 탱을 공격(**도발**). 없으면 HP 최저 아군 공격(처치 집중).
- **몹 광역:** 살아있는 전 아군 타격(공격의 일정 비율, 시작값 60%).
- 탱이 의미를 갖는 이유 = 도발로 유리몸 보호 + 고HP로 버팀.

### 5.7 교착(stalemate) 방지

- 무한 전투 방지: **최대 틱 상한**을 두고 초과 시 **몹 승(플레이어 패)**. 딜이 자가회복·방어를 못 뚫는 교착은 사실상 클리어 불가이므로 패배 처리가 맞다.

---

## 6. 직업 / 관계망 (깊이의 출처 2)

### 6.1 시작 직업 + 시작 제안 수치

| 직업 | HP | 공격 | 타입 | 스피드 | 역할 | 비고 |
|---|---|---|---|---|---|---|
| 노비스 | 70 | 14 | 물리 | 7 | 딜러 | 시작 직업, 약한 만능 |
| 전사 | 115 | 22 | 물리 | 9 | 딜러 | 브루저(튼튼, 딜 낮음) |
| 마법사 | 55 | 32 | 마법 | 8 | 딜러 | 유리대포, **방어 무시** |
| 가디언 | 260 | 10 | 물리 | 5 | 탱커 | 도발, 고HP |
| 사제 | 95 | 6 | — | 7 | 힐러 | 매 턴 회복(기본 30 × 레벨배수) |

- 레벨 배수(시작값): L1=1.0, L2=1.2, L3=1.45 (HP·공격에 곱). **최대 레벨 3.**
- 노비스는 전직으로 전사/마법사/가디언/사제 중 하나(**비가역**).
- 데미지: 물리 = 공격 − 몹 방어(최소 1) / 마법 = 공격(방어 무시).
- 딜러 사이드그레이드 원칙: 전사=튼튼·딜낮음 / 마법사=유리대포·딜높음. 한 직업이 딜·HP 다 높지 않게(지배 방지).
- 마법사 존재 이유: 방어 무시라, 물리면역 속성이 없어도 *방어 높은 적*만으로 이미 "전사 vs 마법사" 선택이 생김.

### 6.2 방향: 직업을 "역할"이 아니라 "상호작용 동사"로

라그나로크의 핵심 재미 = **직업마다 하는 짓 자체가 다름 + 카운터의 카운터.**
단순 딜/탱/힐을 넘어 각 직업에 **버프/디버프/방해 동사**를 부여한다.

방향 예시(확정 아님):
- 마법사 → 적 *방어 깎기* (→ 아군 물리딜 증폭) ← **관계망 첫 동사 후보**
- 사제 → 아군 *피해 감소*
- 음유시인/댄서 → 적 *공속 둔화* / 아군 *공속 증가*
- 어쌔신 → 적 *힐 차단* (자가회복 몹·사제 카운터)
- 세이지/스토커 → 적 *버프 해제 / 스킬 봉인*

### 6.3 카운터의 카운터 (단일 지배 조합 방지)

동사끼리 물고 물리게 설계 → 최강 조합이 수학적으로 존재 불가:
- 자가회복 몹 → 어쌔신(힐차단) 카운터 → 근데 어쌔신은 약함 → 광역 몹이면 먼저 죽음 → 가디언이 보호 → …
- 버프형 몹 → 세이지(버프해제) / 빠른 몹 → 댄서(둔화, 단 딜러 동반 필요).

> **이 관계망은 출전 슬롯이 넓어야 산다.** 3슬롯은 관계의 점 몇 개뿐. 5~6 유닛이 판에 있어야 "방깎→폭딜→공속→힐차단" 연쇄가 의미를 가짐. → **무대(슬롯) 먼저, 배우(동사) 나중.**

### 6.4 2차 전직 (라그나로크식 비가역 분기)

- 노비스 → 1차 → **2차 분기**. 예: 전사 → 로드나이트(단일 폭딜) vs 팔라딘(탱+버프).
- **비가역.** "내가 키운 캐릭터"의 정체성·애착의 핵심.

---

## 7. 몬스터 / 적

### 7.1 설계 모델 — 단일 몹 + 속성 다발

- **적은 매 스테이지 한 마리.** 여러 마리 아님(다대다 타게팅 회피 → 구현 단순).
- 그 한 마리가 **능력·스킬 다발**을 짊어진다. 스테이지가 갈수록 다발의 개수·강도 증가.
- **적은 데이터로 조합을 통한 무한 다양성.**
- 핵심: 재미는 "여러 *마리* 대응"이 아니라 "여러 *요구* 대응". 한 마리여도 서로 다른 대응을 요구하는 속성 다발이면 파티 조합 고민이 살아난다.

**양적 속성 vs 질적 속성 (반드시 구분):**

- **양적 속성**(파워↑·체력↑·방어↑·속도↑): 적을 *더 세게* 만들 뿐, 내 파티 *선택을 안 바꿈* → 이것만으론 파워크리프/정답 맞추기로 수렴.
- **질적 속성**(물리면역·자가회복·반사·광역·회피 등): *특정 답*을 요구 → "어떤 파티냐"가 갈림 = **가위바위보(관계망)의 진짜 연료.**
- **규칙: 속성 풀의 중심은 질적 속성. 양적은 양념. 레어도 높을수록 질적이 붙도록.**

### 7.2 특수 속성 (가위바위보 카운터)

| 속성 | 효과 (시작 제안값) | 카운터 |
|---|---|---|
| 물리면역 | 물리 데미지 0 | 마법사 필수 |
| 마법저항 | 마법 데미지 절반 | 전사 유리 |
| 자가회복 | 매 턴 일정량 회복 | 폭딜 / (향후)힐차단 |
| 데미지반사 | 준 딜의 일정% 반사 | 유리몸 위험 → 탱/저딜 |
| 광역공격 | 전체 타격(공격의 60%) | 탱·힐로 버티기 |
| 회피 | 일정% 회피 | 공격 횟수 확보 |

- 스테이지별 속성 개수는 점증(예: 초반 0~1, 후반 2).
- **상호 배제 규칙:** 물리면역 + 마법저항 동시 등장 금지(양쪽 딜 다 막혀 클리어 불가 방지). 일반화: "모든 딜 타입을 동시에 봉쇄하는 조합"은 막는다.

### 7.3 향후 — 부드러운 카운터

- 속성을 극단 on/off("물리면역=0")가 아니라 **정도(degree)**로("물리 70% 감소") → 카운터가 딱딱한 정답/오답이 아닌 부드러운 선택이 되게.
- 몹에 **상호작용 타입**(버프형/회복형/속도형) 부여 → 더 다양한 카운터 요구.

---

## 8. 밸런싱 방법론

- **눈대중 금지. 시뮬레이터로 측정해 수치 확정.**
- **시뮬 방식:** 전수 조합 승률 측정 — 가능한 파티 전부(중복 허용 조합) × 각 스테이지/속성 × 수백~수천 trial. (전투 엔진과 **함께** 시뮬 스크립트를 만들어, 수치 변경 시 자동 재측정.)
- **검증 기준 3가지:**
  1. 모든 스테이지·모든 속성에 클리어 가능(승률 50%+) 조합이 **존재**하는가.
  2. **단일 지배 조합**이 없는가(모든 상황 1등 = 정답 맞추기).
  3. 속성별로 최적 조합이 **갈리는가**(RPS 작동).
- 스킬 발동이 결정론(가중치 랜덤 아님)이라 시뮬이 분포가 아닌 정확값으로 추적 가능 → 측정 용이.
- ATB 특성상 속도가 행동 빈도로 증폭되므로, **speed를 포함해** 측정한다.

### 8.1 밸런싱 함정 (반드시 피할 것)

1. **방어 스케일링이 물리딜을 죽인다.** 방어를 스테이지마다 올리면 물리 직업이 무력화 → 한 조합으로 수렴. → 방어는 낮게 고정, "방어 트레이드오프"는 *질적 속성(물리면역/마법저항)*으로 표현.
2. **고정 파티 vs 스케일링 몹 = 진행 불가.** 플레이어 파워가 안 따라가는데 몹만 세지면 후반이 전 조합 0%. → 몹 스케일을 눕히거나 성장(경제)이 따라가게.
3. **한 직업이 딜·체력 다 높으면 지배.** → 딜러는 사이드그레이드(6.1).
4. **광역이 과하면 전멸.** 광역=공격 100%면 딜 넣기 전 전멸. → 비율을 낮춰 탱·힐로 버틸 여지 확보(시작값 60%).

> 모든 속성/수치는 "단일 지배도 클리어 불가도 없는" 지점을 시뮬로 찾은 뒤 확정한다.

---

## 9. 빌드 로드맵 (제로부터, 구현 순서 + 이유)

각 단계는 **민망할 만큼 작게 → 측정 → 다음 조각** 리듬으로. 한 번에 다 넣지 말 것.

1. **프로젝트 골격 + 코어 데이터 구조.** 파일 분리(직업데이터 / 몹데이터 / 전투엔진 / 경제 / UI / 시뮬스크립트) + git. 데이터를 코드와 분리(직업·몹·속성은 데이터 테이블로).
2. **ATB 전투 엔진 + 시뮬레이터 동시 구축.** 틱 루프·게이지·평타만으로 시작. 시뮬을 같이 만들어 측정 기반부터 깔기.
3. **직업 + 몹 + 질적 속성(가위바위보).** 평타 기반으로 RPS가 도는지 전수 시뮬로 1차 밸런스 확정.
4. **성장 경제.** 영입/강화/전직(비가역) + 슬롯 확장(체증). 골드·보상·런 리셋.
5. **스킬 시스템.** 다중 스킬 + 플레이어 우선순위 + 마나/쿨. 직업당 스킬 하나씩 검증하며 추가.
6. **상호작용 동사(관계망).** 동사 하나(마법사 방깎)부터 시뮬로 "덧셈→화학반응" 측정, 작동하면 하나씩.
7. **스킬 레벨업.** 위력/마나효율/쿨.
8. **2차 전직 트리.** 비가역 분기.
9. **속성 degree화 + 난이도 곡선 다듬기.**
10. **(선택) 유물 시스템.** 승리 보상 누적(4.3).

---

## 10. 기술 / 작업 환경

- **텍스트 RPG 범위로 충분.** 화려한 그래픽 불필요.
- **처음부터 파일 분리 구조 + git.** 데이터(직업·몹·속성·스킬)는 테이블/설정으로 분리해 밸런스 튜닝이 코드 수정 없이 되게.
- **시뮬레이터를 프로젝트 내 스크립트로** 두어 수치 변경 시 자동 재측정.

---

## 11. 제작 원칙 (메타)

- **민망할 만큼 작게 시작 → 측정 → 한 조각씩.** 스코프 팽창이 완주를 막는 진짜 적. 처음부터 다 만들려 하지 말 것.
- **무대 먼저, 배우 나중.** 의존 순서를 지킬 것(슬롯 → 동사).
- **정체성 안 바꾸기.** "더 깊지만 다른 게임이 되는 길"을 깊이를 위해 택하지 말 것.
- **운 = 경직성 푸는 윤활유지, 결과 흔들기가 아니다.**
- **확신은 행동에서 온다.** 머릿속 검토로 확신을 짜내려 하지 말고, 작게 만들어 결과로 살 것.
- **흥미의 연료 = "내가 만든 게 누군가에게 쓸모 있다"는 효능감.** 만드는 도중에도 그게 느껴지는 방향으로(빨리 보여주고 반응 받기).